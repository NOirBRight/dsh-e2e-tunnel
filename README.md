# @dsh-mobile/e2e-tunnel

NaCl-authenticated DSH tunnel used by DSH Mobile. Direct WebRTC DataChannels plus encrypted Tunnel Fallback over a Host-owned Public Endpoint.

```sh
npm i github:NOirBRight/dsh-e2e-tunnel#v0.1.4
```
